export class Contact {
    public name: string;
    public file: string;
    public pic: string

    constructor(name: string, file: string, pic: string) {
        this.name = name;
        this.file = file;
        this.pic = pic;
    }
}
